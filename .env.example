# KAIROS Configuration
# Copy to .env and customise as needed

KAIROS_LOG_LEVEL=info

# Optional: path to a JSON config file (overrides defaults)
# KAIROS_CONFIG=./config.json

# API settings
KAIROS_API_HOST=0.0.0.0
KAIROS_API_PORT=8000
